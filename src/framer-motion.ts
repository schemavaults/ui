"use client";

export {
  m,
  AnimatePresence,
  domAnimation,
  LazyMotion,
  useAnimate,
  useTransform,
  useMotionValue,
  usePresence,
} from "framer-motion";

export type { MotionValue, AnimationScope } from "framer-motion";
