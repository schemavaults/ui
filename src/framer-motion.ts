"use client";

export {
  m,
  animate,
  AnimatePresence,
  domAnimation,
  LazyMotion,
  Reorder,
  useAnimate,
  useAnimationFrame,
  useTransform,
  useMotionValue,
  usePresence,
  useScroll,
  useSpring,
  useMotionValueEvent,
  useReducedMotion,
} from "framer-motion";

export type { MotionValue, AnimationScope } from "framer-motion";
