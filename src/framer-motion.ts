"use client";

export {
  m,
  AnimatePresence,
  domAnimation,
  LazyMotion,
  useAnimate,
  useAnimationFrame,
  useTransform,
  useMotionValue,
  usePresence,
  useScroll,
  useMotionValueEvent,
  useReducedMotion,
} from "framer-motion";

export type { MotionValue, AnimationScope } from "framer-motion";
