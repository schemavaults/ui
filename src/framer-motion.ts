"use client";

export {
  m,
  animate,
  AnimatePresence,
  domAnimation,
  LazyMotion,
  MotionConfig,
  MotionConfigContext,
  Reorder,
  useAnimate,
  useAnimationFrame,
  useTransform,
  useMotionValue,
  usePresence,
  useScroll,
  useSpring,
  useMotionValueEvent,
  useReducedMotion,
  useReducedMotionConfig,
} from "framer-motion";

export type {
  MotionValue,
  AnimationScope,
  MotionConfigProps,
} from "framer-motion";
